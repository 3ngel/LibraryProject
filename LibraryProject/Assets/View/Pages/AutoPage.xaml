<Page x:Class="LibraryProject.Pages.AutoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LibraryProject.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AutoPage">

    <Grid Background="{StaticResource WhiteColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <UniformGrid Columns="2" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Логин" Style="{StaticResource AutoTextBlock}"/>
            <TextBox x:Name="LoginTextBox" Style="{StaticResource AutoTextBox}" Text="3ngel"/>
            <TextBlock Text="Пароль" Style="{StaticResource AutoTextBlock}"/>
            <PasswordBox x:Name="PasswordTextBox" Style="{StaticResource AutoPasswodBox}" Password="1568493AbC*"/>
            <Button x:Name="EntranceButton" Content="Войти" Style="{StaticResource RegButton}" Click="EntranceButtonClick"/>
            
            <Button x:Name="RegButton" Click="RegButtonClick" Content="Регистрация" Style="{StaticResource RegButton}"/>
        </UniformGrid>
    </Grid>
</Page>
